<div class="newsletter-container">
    <!-- Title -->
    <div class="body-regular"><strong>Subscribe</strong></div>
  
    <!-- Form -->
    <form class="newsletter-form" onsubmit="event.preventDefault(); /* handle subscription */">
      <label for="newsletter-email" class="sr-only">Email Address</label>
      <input
        type="email"
        id="newsletter-email"
        name="newsletter-email"
        placeholder="john.doe@email.com"
        required
      />
      <button type="submit" aria-label="Subscribe">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 19.54 32.24"
          width="1.25rem"
          height="1.25rem"
          preserveAspectRatio="xMidYMid meet"
        >
          <path d="M3.42 0 L0 3.42 L12.62 16.12 L0 28.81 L3.42 32.24 L19.54 16.12 Z" fill="currentColor"/>
        </svg>
      </button>
    </form>
  
    <!-- Disclaimer -->
    <p class="body-sm newsletter-disclaimer">
      By signing up, you agree to receive occasional emails with valuable insurance tips, updates, and offers. We’ll never share your information, and you can unsubscribe at any time.
    </p>
  </div>
  
  <style>
    .newsletter-container {
      display: flex;
      flex-direction: column;
      width: 100%;
    }
  
    .sr-only {
      position: absolute !important;
      width: 1px; 
      height: 1px; 
      padding: 0; 
      margin: -1px; 
      overflow: hidden; 
      clip: rect(0,0,0,0); 
      border: 0;
    }
  
    .newsletter-form {
      display: flex;
      align-items: flex-start;
      gap: 0;
    }
  
    .newsletter-form input[type="email"] {
      flex: 1 1 auto;
      padding: 0.75rem 1rem;
      border: none;
      border-radius: 0.5rem;
      font-size: 1rem;
      box-sizing: border-box;
      background-color: #fff;
      color: #000;
      height: 2.75rem;
      margin-bottom: 1rem;
      outline: 2px solid transparent;
      transition: all 0.3s ease-in-out;
    }
    .newsletter-form input[type="email"]:hover {
      outline: 2px solid var(--primary-300);
      transition: all 0.1s ease-in-out;
    }
    .newsletter-form input[type="email"]:focus {
      outline: 2px solid var(--primary-300);
      transition: all 0.1s ease-in-out;
    }
  
    .newsletter-form button {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border: none;
      with: 2.75rem;
      height: 2.75rem;
      padding: 0 1rem;
      border-radius: 0.5rem;
      cursor: pointer;
      margin: 0 0 0 0.75rem;
      transition: background-color 0.3s ease;
    }


    .newsletter-disclaimer {
      margin: 0;
      line-height: 1.4;
      opacity: 0.9;
    }
  </style>
  